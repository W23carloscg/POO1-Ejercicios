import javax.swing.JOptionPane;
public class TestUsuarioJpane {
	public static void main (String[] args) {
		String password = JOptionPane.showInputDialog("Introduce la contraseña del usuario");
		//Wrapper
		/**
		if (passwordNumero == usuario.getPassword()) {
			 JOptionPane.showInternalMessageDialog(null, "mensaje ", JOptionPane.PLAIN_MESSAGE);
		}
	    String informacionSalida = "Valor introducido" + inputValue;
	    JOptionPane.showInternalConfirmDialog(null, informacionSalida, message)
	
		do {
			String login = JOptionPane.showInputDialog("Introduce el login del usuario");
			int passwordNumero = Integer.parseInt(password);
		//} while (passwordNumero == usuario.getPassword());
		//JOptionPane.showInternalMessageDialog(null, usuario "mensaje", JOpionPane.PLAIN_MESSAGE);
	}
	**/
	}
}
